<div class="row col-md-4 col-md-offset-4">
  <% @definitions.each do |definition| %>
    <p>
      <%= link_to "#{definition.tense.titleize} Tense Verbs", next_question_path(tense: definition.tense), class: "btn btn-lg btn-success centerize" %>
    </p>
  <% end %>
</div>
